import styled from 'styled-components'

export const Wrapper = styled.nav`
  position: sticky;
  width: 200px;
  height: max-content;
  top: 100px;
  margin-top: 30px;

  .h4 {
    margin: 15px 0;
    font-weight: 700;
  }

  .card-sidebar {
    p {
      margin: 0;
    }
  }
`
